{
  "name": "PNP",
  "version": "0.0.1",
  "description": "PNP is a networking protocol based on WebSockets that supports an asynchronous API rather than an event-driven one.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Picsum",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}
